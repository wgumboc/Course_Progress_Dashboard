import Header from './components/Header'
import Button from './components/Button'

function App() {
  return (
    <div className="container">
      <Header title = "Weighted Average Calculator"/>
      <Button color = 'rgb(41, 97, 153)' text = 'Add Course' />
    </div>
  );
}

export default App;
